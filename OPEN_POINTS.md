# Open Points — Rolling List

Living document mirroring the rolling list maintained in chat. Update on every
working session. Status: 2026-07-24.

## Open

| # | Point | Owner / next step |
|---|-------|-------------------|
| 1 | **Git push** — commit(s) exist locally only; sandbox has no GitHub credentials | Sören: `cd Downloads/Projects/PMI/PMI-Reporting-Agent && git push -u origin main` |
| 2 | **OpenAI vs Claude key** — mock mode active; provider decision pending | Team; see `docs/LLM_Provider_Comparison.md` |
| 4 | **Training data for conflict-resolution skill** — synthetic vs proxy (SEC EDGAR) vs hybrid | Team; see `docs/TrainingData_Decision.md` + slide |
| 5 | **ECTSum ticker overlap** — accept 310-ticker train/test overlap or re-split ticker-disjoint | Team; affects eval validity |
| 6 | **AMI corpus download** — blocked from sandbox; manual download needed if transcript scope confirmed | Sören (links in Google Doc "H2 Deep Dive" §3) |
| 7 | **Senior Manager review gate** — stated guardrail, not an enforced workflow step; candidate step 8. Now has a concrete build spec: `docs/RAG_Flywheel_Engineering_Handoff.md` M3 (review UI + endpoints), plus M2/M4 (persists approvals, feeds the RAG corpus) and a static design mockup (`static/review_mockup.html`) | Engineering — M1 (frozen gold set) should land first, then M2–M4 |
| 8 | **Repo size** — data/ectsum ≈ 56 MB; fine for GitHub, consider LFS if data grows | Watch |
| 9 | **"Dataset augmentation plan" reconciliation** — a MASTER.md version reviewed 2026-07-18 (`docs/DataIngestion_CriticalReview.md`) describes a `katja-dataset` branch, an M0–M6 status table, and named scripts (`scripts/dataset/prep_ectsum.py`, `render_to_pmi_formats.py`) that exist in none of `main`, `origin/katja`, or `origin/soeren` — confirm where this work actually lives (uncommitted local files elsewhere, or aspirational text) before trusting the claimed "M0 done / M1 paused mid-build" status | Sören / whoever owns the `katja-dataset` work |
| 10 | **Gap 1 (multi-format structured extraction) has no funded data layer** — per the same 2026-07-18 review, the dataset plan as scoped covers report *generation* only; extraction (`excel.py`/`word.py`/`powerpoint.py`/`pdf.py`/`html.py`/`matching.py` — the bulk of the codebase and, per `MASTER.md`'s own bug history, the actual source of most defects) has zero data-layer coverage. `TrainingData_Decision.md` already names SEC EDGAR XBRL↔HTML/Ex-99.1 as the near-free real-data candidate; `UC2_V2_SingleAgent_Definition.md` §5 also names FUNSD/DocVQA/PubTables-1M as generic analogs, not yet researched to the same rigor as the survivor corpora | Team — promote to a first-class, funded pipeline row, not a future addendum |
| 12 | **QMSum not actually downloaded** — `TrainingData_Decision.md` claims "Downloaded & verified (local, 102 MB)"; a 2026-07-18 search of this project folder and repo (incl. `origin/katja`) found no `qmsum` directory or JSON files anywhere, the same claimed-vs-actual pattern already caught for the `katja-dataset` branch (#9). Pull from `https://github.com/Yale-LILY/QMSum` (`data/ALL`) into `data/qmsum/` before D1 execution starts | Sören / whoever owns the data layer, see `docs/TrainingData_UseCase_Fit_Analysis.md` Demand 1 |
| 13 | **GovReport (Huang et al. 2021, NAACL, CC BY 4.0) as a third generation-tier source** — a 2026-07-18 `pmi-deep-research` pass found GovReport (19,466 real GAO/CRS report-to-summary pairs) has a closer register to structured status reporting than ECTSum or QMSum, and a more permissive license than ECTSum. Not a replacement for either existing corpus, proposed as an addition targeting the executive/SteerCo tier specifically | Team decision, see `docs/TrainingData_UseCase_Fit_Analysis.md` §6 |
| 14 | **Specialized extraction model vs. RAG/ICL-only, indefinitely** — the 2026-07-24 D3 pass (`docs/TrainingData_Decision.md` §8) recommends RAG/in-context learning fed by the SM-review gate as the near-term data/learning strategy, since the agent has no training loop today. Whether the team ever wants a smaller, purpose-trained extraction model (which would revive candidate 3 — Snorkel-style weak supervision over Gap-1 EDGAR/real documents, item #10) is now the open follow-on decision | Team, no urgency — revisit once the RAG/ICL loop is running and its cost/accuracy is measured |
| 15 | **M1 — frozen gold set still doesn't exist** — `docs/RAG_Flywheel_Engineering_Handoff.md` M1 (15–30 real/anonymized PMI examples) blocks meaningful evaluation of every other milestone and was already called "non-negotiable" back in `TrainingData_Decision.md` §6 — still not built. Rev. 2 adds two hard requirements: a second annotator on a ≥5-case subset with a reported reliability coefficient (Cohen's κ / exact-match + MAE), and the anonymization procedure in §0.5. **Must be frozen before M1a (synthetic generator) is designed**, else generator and gold set become correlated by construction | Sören/team, not a coding task — needs real material |
| 16 | **Excerpt vs. location-only provenance in the SM-review UI** — `docs/RAG_Flywheel_Engineering_Handoff.md` M3: `SourceRef` gives file/sheet/slide location, not a literal quoted text excerpt; showing reviewers the actual source text needs `app/extractors/*.py` extended to retain a raw snippet per record first | Engineering — decide scope (location-only vs. add snippet extraction) before M3 build starts; tracked as task 3.0 in `docs/IMPLEMENTATION_STATUS.md` |
| 17 | **H2's collection half is out of scope and must be declared** — per the 2026-07-24 academic critical review of the UC3 slide: the slide ranks "automated status collection directly from workstream leads (form/Jira/transcript)" as the highest-demand addition, and the build does not implement it — the agent presupposes a human already gathered and uploaded the files. H2 claims both *collecting* and *generating*. Every deliverable citing this work must state that only the generation half is addressed | Team — either accept as a written limitation (current plan, `RAG_Flywheel_Engineering_Handoff.md` §0.1) or scope a collection module |
| 18 | **No manual baseline exists** — "reduces manual effort" is unfalsifiable without a measured comparator. New milestone M0 (`RAG_Flywheel_Engineering_Handoff.md`) requires 2–3 team members to produce a report manually with recorded timings, scored against the same gold labels | Sören/team — not a coding task; blocks any effort-reduction claim |

## Decided (log)

| Date | Decision |
|------|----------|
| 2026-07-09 | Frontend: plain HTML/JS served by FastAPI (over React) — fastest to test instance |
| 2026-07-09 | LLM: mock mode first; OpenAI activates via `OPENAI_API_KEY` env switch |
| 2026-07-09 | ECTSum checked into repo under `data/ectsum/` (1681/249/495 verified) |
| 2026-07-09 | Separate repo for V2 (per UC2_V2 §8), cloned from SoerenM2345/PMI-Reporting-Agent |
| 2026-07-09 | Conflict resolution implements both spec options: source priority (default) + ask-user mode |
| 2026-07-18 | D1 (transcript ingestion / QMSum+AMI relevance) resolved: keep both ECTSum and QMSum in scope. Usage plan (split-to-format rendering fixtures, coding-agent demands) in `docs/TrainingData_UseCase_Fit_Analysis.md` §7 |
| 2026-07-24 | D3 resolved: `pmi-deep-research` 8-step protocol run on the data-utilization/evaluation-methodology decision (`docs/TrainingData_Decision.md` §8). Recommendation: RAG/in-context learning (Lewis et al. 2020) fed by the mandatory SM-review gate (Settles 2009 active-learning framing; "Agent-in-the-Loop" 2025), evaluated prequentially (Gama et al. 2014) against the frozen held-out gold set — not k-fold, not one pooled dataset. Pure unsupervised pattern-finding screened out as unsupported in the literature for structured extraction. |
