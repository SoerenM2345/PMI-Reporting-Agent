__pycache__/
*.pyc
.pytest_cache/
.coverage
htmlcov/
.DS_Store

# environments
.env
.env.bak
venv/
.venv/
.venv-dataset/
.venv-dataset/

# runtime artefacts
output/
storage_data/

# eval harness run output — regenerable per run.json's own recipe; a handful of early
# runs are committed as evidence (scripts/eval/runs/clean_Z_*, with_errors_Z_*), future
# ones (5 reps x 2 corpora x 3 conditions, each with real deck/report files) are not
scripts/eval/runs/*
!scripts/eval/runs/clean_Z_1_4dccacb2/
!scripts/eval/runs/with_errors_Z_1_40f42e80/
!scripts/eval/runs/results_Z_smoke.csv

# frontend build artefacts — the Docker image builds these itself
frontend/node_modules/
frontend/dist/
frontend/CLAUDE.md

CLAUDE.md
AGENTS.md

# the source spec — kept out of the repo, as before
agent.md
agent_description.txt
