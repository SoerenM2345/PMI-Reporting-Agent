# Copy to .env (or export) to enable real LLM calls.
# Without a key the agent runs in deterministic mock mode — fully functional,
# summaries are template-based instead of LLM-written.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.5
